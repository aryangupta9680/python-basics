# Take input from user for length and breadth
length = eval(input("Enter the length of the rectangle: "))
breadth = eval(input("Enter the breadth of the rectangle: "))

# Calculate area
area = length * breadth

# Display the result
print("Area of the rectangle is:", area)
